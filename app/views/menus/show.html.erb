<h1><%="Menu of" + " " + @restaurant.name%> </h1>
<h2><%=@date.today? ? "For today:" : "for " + @date.strftime("%A %d.%m.%y")%></h2>

<% @food.each do |food_item|%>
	<h2><%= food_item.name%></h2>
<% end%>